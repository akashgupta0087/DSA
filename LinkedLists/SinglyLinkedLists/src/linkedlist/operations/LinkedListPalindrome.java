package linkedlist.operations;

import linkedlist.basic.Node;

public class LinkedListPalindrome {

	/*
	 * public boolean isPalindrome(Node temp) { Node head = temp; while(temp !=
	 * null) { temp = temp.getNext(); }
	 * 
	 * 
	 * }
	 */

}
