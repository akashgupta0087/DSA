package sorting.mergesort;

public class MergeSort {
	
	public int[] sort(int start, int end, int[] arr) {
		if(start < end) {
			int[] ogArr = arr;
			int mid = (start + end) / 2;
			ogArr = getArr(start, mid, arr);
			int[] leftSorted = sort(start, mid, ogArr);
			
			ogArr = getArr(mid+1, end, ogArr);
			int[] rightSorted = sort(mid+1, end, ogArr);
			ogArr = merge(leftSorted, rightSorted);
		}
		return arr;
	}
	
	private int[] merge(int[] leftArr, int[] rightArr) {
		int n = leftArr.length + rightArr.length;
		int[] resultArr = new int[n];
		return resultArr;
	}
	
	private int[] getArr(int start, int end, int[] arr) {
		int n = (end-start) + 1;
		int[] newArr = new int[n];
		for(int i = 0; i < n; i++) {
			newArr[i] = arr[i];
		}
		return newArr;
		
	}
	
}
